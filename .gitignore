# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/

# macOS
.DS_Store

# Editors
.idea/
.vscode/
*.swp
*.swo

# Centella run artifacts — created per-run in target repos, never tracked.
.centella/
# Top-level log when the CLI is teed (`./centella "..." | tee centella.log`).
centella.log

# Local Claude Code settings / cache — per-machine, never tracked.
.claude/

# Working notes — kept locally as a reference, not a deliverable.
foo.txt

# Test artifacts
.pytest_cache/
.coverage
htmlcov/
